public class Hmm {
    public static void main() {
        Thing thing;
        thing == (Thing1) a;
        Thing thing = (ok.clone()).stuff();
        NetworkInfo activeNetwork = ((ConnectivityManager) context.getSystemService(
                                                                                    Context.CONNECTIVITY_SERVICE)).getActiveNetworkInfo();


    }


}
